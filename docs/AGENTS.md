# docs/ — AGENTS.md

## Purpose

This folder drives **Phase 1** of the vibecode workflow: generating the foundational documents before any code is written.

## Folder Layout

```
docs/
├── 00_intake/        ← Start here: fill out the questionnaire
├── 01_prompts/       ← Prompt files to paste into any AI tool
└── 02_outputs/       ← Save AI-generated outputs here
```

## Order of Operations

1. **Fill in `00_intake/intake_questionnaire.md`** — this is the source of truth for all `[[PLACEHOLDER]]` tokens
2. **Work through `01_prompts/` in order** (01 → 09):
   - Fill placeholders from the intake answers
   - Paste the prompt into your AI tool
   - Save the output to `02_outputs/` with a matching filename (e.g. `02_outputs/01_prd.md`)
   - Prompts 08 and 09 require the **Figma MCP server** and the project's Figma file URL
3. **Fill in the design registry** (`02_outputs/05_design_registry.md`) with Figma URLs for each feature/flow
4. **Review all outputs** before starting Phase 2
5. **Seed `adr/` and `memory/`** from the architecture and implementation plan docs

## Output Files Reference

| File | Generated by prompt | Purpose |
|------|--------------------|---------| 
| `01_prd.md` | `01_prd.prompt.md` | Product Requirements Document |
| `02_functional_spec.md` | `02_functional_spec.prompt.md` | Functional specification |
| `03_tech_architecture.md` | `03_tech_architecture.prompt.md` | Technical architecture |
| `04_api_spec.yaml` | `04_api_spec.prompt.md` | OpenAPI spec (contract source of truth) |
| `05_design_registry.md` | Filled manually | Feature → Figma URL mapping; skip flags per feature |
| `06_implementation_plan.md` | `05_implementation_plan.prompt.md` | Phased implementation plan |
| `07_design_system.md` | `07_design_system.prompt.md` | Design tokens, typography, color palette |
| `08_design_gap_report.md` | `09_design_requirements_validation.prompt.md` | Figma vs. requirements gap analysis |

## Rules for AI Tools

- Do **not** modify files in `01_prompts/` — these are prompt templates
- Place all generated documents in `02_outputs/`
- The OpenAPI spec (`04_api_spec`) in `02_outputs/` is the contract for contract tests in `tests/contract/`
- The design registry (`05_design_registry.md`) is maintained manually by the team — update it when new designs arrive
- If a document is revised, save the new version to `02_outputs/` (overwrite is fine; git history tracks changes)
